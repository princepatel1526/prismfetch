# prismfetch
A `neofetch`-style system-info tool for **PrismOS**, written in Rust. Prints a colored block-grid logo next to key system stats, plus gradient usage bars for CPU, memory, disk, and temperature — matching the PrismOS Aurora theme.
